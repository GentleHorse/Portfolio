import { ImageProperties } from './image.js';
export type VideoProperties = Omit<ImageProperties, 'src'> & InternalVideoProperties;
type InternalVideoProperties = {
    /**
     * when a HtmlVideoElement is provided to src, properties like `volume`, ... are not applied
     */
    src?: string | MediaStream | HTMLVideoElement;
    volume?: number;
    preservesPitch?: boolean;
    playbackRate?: number;
    muted?: boolean;
    loop?: boolean;
    autoplay?: boolean;
};
export declare function updateVideoElement(element: HTMLVideoElement, { src, autoplay, loop, muted, playbackRate, preservesPitch, volume }: InternalVideoProperties): void;
export declare function setupVideoElementInvalidation(element: HTMLVideoElement, invalidate: () => void): () => void;
export {};
