import { WebGLRenderer } from 'three';
import { Root } from './root.js';
import { FullscreenProperties } from '../components/index.js';
import { AllOptionalProperties } from '../properties/index.js';
import { FontFamilies } from '../text/index.js';
export declare class Fullscreen extends Root {
    private renderer;
    private distanceToCamera?;
    private parentCameraSignal;
    private readonly sizeX;
    private readonly sizeY;
    private readonly pixelSize;
    private readonly transformTranslateZ;
    constructor(renderer: WebGLRenderer, distanceToCamera?: number | undefined, properties?: FullscreenProperties, defaultProperties?: AllOptionalProperties, fontFamilies?: FontFamilies, requestRender?: () => void);
    /**
     * must be called when the screen size changes
     */
    updateSize(): void;
    getStyle(): undefined | Readonly<FullscreenProperties>;
    setStyle(style: FullscreenProperties | undefined): void;
    setProperties(properties: FullscreenProperties | undefined): void;
}
